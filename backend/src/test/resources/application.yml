spring:
  datasource:
    url: jdbc:postgresql://127.0.0.1:5432/postgresdb
    username: "postgresuser"
    password: "postgrespassword"
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: validate